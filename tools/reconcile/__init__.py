"""Post-run reconciliation for the synthetic demo.

Reads the raw source files and queries the loaded database, then checks that the
run is complete and consistent. Additive demo tooling: nothing here changes the
pipeline, and reports contain counts and synthetic IDs only.
"""
